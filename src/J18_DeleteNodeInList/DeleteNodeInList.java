package J18_DeleteNodeInList;

/**
 * 面试题18（一）：在O(1)时间删除链表结点
 * <p>题目：给定单向链表的头指针和一个结点指针，定义一个函数在O(1)时间删除该
 * 结点。
 */
public class DeleteNodeInList {
    public ListNode deleteNode(ListNode head, ListNode tobeDelete) {
        // 如果只有表头节点，则删除表头节点
        // 如果要删除的节点不是尾节点，则将后一节点覆盖要删除的节点，再删除后一节点
        // 如果要删除的节点是尾节点，则从头结点遍历到尾节点的前一个节点，再删除尾节点
        return null;
    }
}
